# Repository Instructions

- Do not start local dev servers in this repository. Avoid commands such as `npm run dev`, `pnpm run dev`, `yarn dev`, `astro dev`, or similar server/watch commands unless the user explicitly grants permission in the current conversation.
